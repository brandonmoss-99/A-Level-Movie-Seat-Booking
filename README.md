# A-Level-Movie-Seat-Booking
Coursework for my Computer Science A-Level, written around 2016

To run, first compile with `javac -d . *.java`

Once compiled, it can be run with `java computingproject.Computingproject`